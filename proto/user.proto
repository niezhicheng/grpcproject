syntax = "proto3";
package user;
option go_package="../";

service UserService {
    rpc SayHello (UserRequest) returns (UserResponse){}
}

message UserRequest {
    string username = 1;
    string password = 2;
}

message UserResponse {
    string msg = 1;
    int64 code = 2;
}
